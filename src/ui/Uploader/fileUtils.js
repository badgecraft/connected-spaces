import styled from '@emotion/styled';

export const Root = styled('div')({
    minHeight:      192,
    display:        'flex',
    alignItems:     'center',
    justifyContent: 'center',
    width:          '100%',
    flexDirection:  'column',
});

export const FuRoot = styled('div')({
    display:    'flex',
    width:      '100%',
    alignItems: 'center',
    textAlign:  'left',
});
